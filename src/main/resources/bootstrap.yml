spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: configmap-server-kubernetes
      reload:
        enabled: true
        mode: event